public class Trick {
    // Represents a trick in the game.
    // It tracks the cards played by each player and determines the winner of the trick.

    private Card[] cardsPlayed;

    public Trick(Card leadCard, int playerNum) {
        /*switch (leadCard.getSuit().equals("Diam")) {

         }*/
    }
}
